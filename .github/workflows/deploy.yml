name: Deploy to Public GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to PUBLIC GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.ACCESS_TOKEN }}
          external_repository: Admin-Dekstop/Hosting-TanyaData
          publish_branch: gh-pages
          publish_dir: ./  # Ganti sesuai folder file kamu
          force_orphan: true
          commit_message: "🚀 Deploy from private repo"
